server.port=8080
spring.application.name=tournament-server

# Client bot settings (override with --key=value when running ClientApp)
tournament.server.host=localhost
tournament.server.port=8080
tournament.name=AlmostFull
robot.name=RemoteRobot
# robot.ip= (leave blank to auto-detect; set explicitly when on a different machine)